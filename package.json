{
  "name": "",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node ./bin/www",
    "devserver": "DEBUG=* node --debug-brk  ./bin/www",
    "prepublish": "pre-commit install"
  },
  "dependencies": {
  },
  "devDependencies": {
    "eslint": "3.12.2",
    "eslint-config-google": "0.7.1"
  }
}
